
        {{nameCapital}}: Npc.extend({
            init: function(id) {
                this._super(id, Types.Entities.{{nameUppercase}}, 1);
                // this.idleSpeed = 100;
            }
        }),
        /* npcs */